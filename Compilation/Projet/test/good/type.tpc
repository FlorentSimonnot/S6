const number = 3;
int somme, produit, difference, quotient, reste; 
int resultat;
char letter, digit, punct; 
long res; 

int main(void){
	somme = 12; 
	produit = 24; 
	quotient = 24/12; 
	somme = produit + quotient; 
	print(somme);
	difference = somme - 1; 
	produit = somme * somme; 
	print(produit);
	reste = somme + produit; 
	produit = (-somme + produit) / (somme + reste) % 5;

	resultat = (int) letter;
	res = (long) number + somme;
	print(res);
	return 0;
}