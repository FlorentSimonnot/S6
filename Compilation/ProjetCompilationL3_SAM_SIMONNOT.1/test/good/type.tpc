#define MACRO 30

const number = 3;
int somme, produit, difference, quotient, reste; 
int resultat;
char letter, digit, punct; 

int main(void){
	somme = 12; 
	produit = 24; 
	quotient = 24/12; 
	somme = produit + quotient; 
	difference = somme - 1; 
	produit = somme * somme; 
	reste = somme + produit; 
	produit = (-somme + produit) / (somme + reste) % 5;

	resultat = (int) letter;

	print(number);

	return 0;
}